# Set method

se = {1, 2, 3, 4, 5}
print(se)
# Set does not allow duplicate values

# Set does not maintain the order of elements

# Set is mutable but the elements of a set must be immutable

# Set does not support indexing and slicing

# Set is a collection of unordered and unindexed elements

# Set is a collection of unique elements (allows Multiple data types)

s = { 1, 2, 3, 4, 5, "Hello", (1, 2), True}

let = set()
print(type(let))